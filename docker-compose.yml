version: "3.7"

services:
  app:
    build: .
    image: coinbasepro-notifier:latest
    volumes:
      - "./data:/app/data"
    env_file:
      - ".env"
    ports:
      - "8080:8080"
    restart: unless-stopped
